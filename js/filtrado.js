const filtro =()=>{
    //Recogeremos los elementos
    //Enviar a la funcion redirect con el valor del elemento
    redirect();

}

const redirect = ()=>{
    //Redirigimos la url con los elementos
    location.href
}